#include <cstdio>
#include <vector>
using namespace std;

struct edge{
	int start;
	int end;
	int cost;
};
vector<edge> v;

int main()
{
	int n, m;
	scanf("%d &m", &n, &m);

	for (int i = 0; i < m; i++)
	{
		int a, b, c;
		scanf("%d %d %d", &a, &b, &c);
	}

}