#include <cstdio>

int input[101][101];

int main()
{
	int n, m;
	scanf("%d %d", &n, &m);

	for (int i = 1; i <= n; i++)
	{
		for (int j = 1; j <= m; j++)
			scanf("%d", &input[i][j]);
	}

	int max = 0;
	for(int i=1;i<=)
}